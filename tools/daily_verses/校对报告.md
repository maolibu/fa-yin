# 每日偈颂数据恢复与校验记录

## 2026-08-24 迁移恢复

迁移盘点时，仓库 README 曾称本目录应有 `每日偈颂.csv` 与包含
“700 处修改记录”的校对报告，但在新旧工作区、Git 历史与迁移归档中
均未找到这两份原始文件。因此，不得将本文档表述为已找回原始的
700 处人工修改明细。

现用 `data/db/verses.json` 由 Git 跟踪，包含 365 条数据，迁移冻结 SHA-256 为：

`51d864334a42af558abb76a88c07a8a4a7467fd70ab998864bbb6e83a480a4c4`

本目录的 `每日偈颂.csv` 是以该 JSON 为权威冻结版恢复的：每条
`lines` 用全角分隔符 `｜` 连接，`source` 原样写入出处列。恢复没有
新增、删除、校改或猜测任何偈颂内容。

自动化验证见 `tests/test_daily_verses.py`：CSV 重新转换后必须与冻结 JSON
的 365 条 `id` / `lines` / `source` 逐字段完全一致。
